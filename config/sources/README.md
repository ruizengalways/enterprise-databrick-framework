# Source-level configuration

Put connection-agnostic source defaults here: owner, source technology, retention expectations, source timezone, CDC/log retention and supported bootstrap method. Secrets and credentials never belong in Git.
