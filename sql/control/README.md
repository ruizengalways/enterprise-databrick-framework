# Control schema

Executable control-table DDL is generated by `edp_framework.operations.control_tables` so environment catalog names can be validated and injected safely. This directory is reserved for human-readable migrations that cannot be expressed safely through the bootstrap package.
